export interface Task {
    _id: string;
    title: string;
    description: string;
    status: string;
  }
  
  export interface TaskFormData {
    title: string;
    description: string;
    status: string;
  }